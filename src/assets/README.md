# Aset kelas

Tempatkan aset lokal tambahan di folder ini, lalu import dari komponen.
Logo 3D saat ini dibuat secara prosedural di `src/components/Scene3D.jsx`, sehingga tidak membutuhkan model atau tekstur eksternal.
Foto anggota menggunakan URL di `src/data/siswa.json`, dengan fallback avatar inisial.
